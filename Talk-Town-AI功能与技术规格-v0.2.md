# Talk Town AI 功能与技术规格

> 版本：v0.2  
> 状态：已被《Talk Town 完整 AI 产品 PRD v1.0》替代，仅保留历史设计  
> 更新日期：2026-08-22

## 1. 文档目的

本文档保存 Talk Town 已完成的 AI 功能、状态机、接口、非功能需求和评测设计。产品假设、首轮验证范围、用户故事与发布决策以独立的产品 PRD 为准；若二者发生范围冲突，以更新版本的产品 PRD 为准。

## 2. 已确认的产品结论

### 2.1 产品定位

Talk Town 是一款 AI 场景化英语学习产品，帮助近期准备出境旅游、英语基础薄弱的成年人，通过完成真实交流任务，在短时间内获得可以立即使用的英语能力。

### 2.2 核心价值

用户获得的结果不是“学完一节英语课”，而是“能够完成一次真实英语交流任务”。

### 2.3 MVP 验证目标

验证“通过 AI 场景化任务进行英语训练，是否能让目标用户完成任务并感受到能力提升”。

### 2.4 MVP 主链路

1. 用户输入学习目标。
2. AI 理解并结构化目标。
3. 系统生成学习路线。
4. 用户进入咖啡店点餐场景。
5. NPC 用英文提问，用户主要通过输入简短英文进行问答互动。
6. AI 判断回答、解释问题并推进任务。
7. 用户完成点餐任务并看到结果。

### 2.5 MVP 不包含

- 真人语音聊天
- 发音评分
- 社交功能
- 复杂课程体系
- 商城
- 游戏化积分体系

## 3. 功能优先级

### P0：必须具备，否则无法验证核心假设

| 编号 | 功能 | 核心作用 |
| --- | --- | --- |
| FR-01 | 学习目标输入 | 获取用户当前要解决的真实问题 |
| FR-02 | AI 目标理解与确认 | 将自然语言目标转为可用于生成路线的结构化信息 |
| FR-03 | 学习路线生成与场景入口 | 把模糊目标变成可执行的任务路径 |
| FR-04 | 场景任务初始化 | 建立咖啡店点餐的目标、角色和任务状态 |
| FR-05 | NPC 互动 | 让用户在模拟交流中完成任务 |
| FR-06 | 用户回答理解与判定 | 判断用户是否表达了当前步骤所需意图 |
| FR-07 | 错误解释与修正 | 帮助用户理解问题并立即重试 |
| FR-08 | 剧情推进与任务完成 | 保证互动围绕真实任务向前推进 |
| FR-09 | 任务结果与最小学习记录 | 呈现成功反馈并记录本次训练结果 |
| FR-10 | AI 异常降级 | 在模型失败或输出不合规时保证任务仍可继续 |

### P1：核心验证通过后再做

- 根据历史错误生成针对性练习
- 动态难度调整
- 跨场景用户学习记忆
- 更多旅行场景的完整任务内容

上述能力符合长期方向，但不是咖啡店单场景闭环成立的前置条件。

## 4. P0 功能需求

## FR-01 学习目标输入

### 为什么需要

用户通常只知道自己“要去旅游”，不知道需要学习哪些英语内容。产品必须从用户真实目标出发，而不是要求用户先理解课程分类。

### 解决的用户问题

- 不知道应该学什么。
- 无法把现实需求转换成学习计划。

### 使用流程

1. 用户进入产品首页。
2. 系统提示用户描述近期要完成的事情，例如“我要去美国旅游”。
3. 用户提交一段自然语言文本。
4. 系统校验输入并进入目标理解。

### AI 输入

- 用户原始目标文本
- 产品支持的目标范围：出境旅游英语
- 界面语言：简体中文
- 匿名用户 ID 及本次会话上下文

### AI 处理

本功能本身只负责采集与基础校验；语义处理由 FR-02 完成。

### 输出结果

- 合法的用户目标文本
- 输入时间和一次目标请求标识

### 规则与异常

- 空输入不可提交。
- 纯空格、明显乱码或超过长度上限的内容应提示修改。
- 对非旅游目标不直接生成无关课程，应明确当前版本能力边界并允许重新输入。
- 不要求用户填写完整问卷。

### 验收标准

1. 输入“我要去美国旅游”后可以成功提交并进入目标理解。
2. 空输入和纯空格输入不能提交，并显示可理解的提示。
3. 输入明显非旅游目标时，系统不会生成伪造的旅游路线，而是提示当前版本主要支持出境旅游英语。
4. 用户可以返回修改原始目标。

## FR-02 AI 目标理解与确认

### 为什么需要

用户表达通常模糊。系统需要把自然语言转换为稳定、可解释的结构，才能生成相关学习路线。

### 解决的用户问题

- 用户不知道如何描述详细学习需求。
- AI 误解目标会导致后续内容与真实需求不相关。

### 使用流程

1. 系统接收用户目标。
2. AI 提取旅行目的、目的地、时间信息和核心场景需求。
3. 系统用用户能理解的语言展示识别结果。
4. 用户确认或修改结果。
5. 确认后进入路线生成。

### AI 输入

```json
{
  "raw_goal": "我要去美国旅游",
  "supported_domain": "outbound_travel_english",
  "locale": "zh-CN"
}
```

### AI 处理

- 判断目标是否属于出境旅游英语范围。
- 提取已明确的信息，不猜测用户未提供的关键事实。
- 对缺失但不是路线生成前置条件的信息保留为空。
- 将结果输出为固定结构，不输出自由格式课程正文。

### 输出结果

```json
{
  "in_scope": true,
  "goal_type": "outbound_travel",
  "destination": "美国",
  "departure_time": null,
  "recommended_scene_categories": ["机场", "酒店", "餐饮", "购物"],
  "summary": "为美国旅行准备常见场景英语"
}
```

### 规则与异常

- AI 不得把未提供的信息显示为已确认事实。
- 低置信或有多种解释时，应请求用户确认，而不是静默选择。
- 结构化输出解析失败时执行一次重试；仍失败则进入 FR-10 降级流程。

### 验收标准

1. 对“我要去美国旅游”，系统正确识别为出境旅游目标，并识别目的地为美国。
2. 用户未提供出发时间时，结果保持为空，不虚构日期。
3. 用户可修改识别错误的信息后再确认。
4. 非支持范围的目标不会进入路线生成。
5. AI 返回非结构化内容时，前端不直接展示原始模型输出。

## FR-03 学习路线生成与场景入口

### 为什么需要

用户需要看到从“旅行”到“具体交流任务”的转换，明确接下来应该做什么。

### 解决的用户问题

- 学习目标过大，不知道先学什么。
- 传统课程与真实任务之间缺少明确对应关系。

### 使用流程

1. 用户确认结构化目标。
2. 系统生成按旅行过程组织的场景路线。
3. 用户查看场景名称、任务结果和可用状态。
4. 用户进入咖啡店点餐任务。

### AI 输入

- FR-02 的结构化目标
- MVP 支持的场景目录
- 路线排序规则
- 当前可体验的场景能力

### AI 处理

- 从受控场景目录中选择与目标相关的场景。
- 按典型旅行过程排序。
- 为每个场景生成“用户完成后能做什么”的任务描述。
- 不生成当前产品无法实际承载的可用场景。

### 输出结果

- 路线标题
- 场景列表及顺序
- 每个场景的任务目标
- 可用、未开放或已完成状态

### 规则与异常

- 路线必须至少包含一个可进入场景。
- 咖啡店点餐是本版首个完整可体验场景。
- 路线可以展示机场、酒店、购物等未开放场景，但必须明确标记“暂未开放”，且不能进入。
- AI 生成失败时使用预置的旅游路线降级。

### 验收标准

1. 对已确认的旅游目标，系统能返回结构有效的路线。
2. 用户能够从路线进入咖啡店点餐场景。
3. 每个展示的场景都有明确、结果导向的任务描述。
4. 不可用场景不会被误标为可体验。
5. AI 失败时，用户仍能通过预置路线进入咖啡店任务。

## FR-04 场景任务初始化

### 为什么需要

自由聊天容易偏离学习目标。进入互动前必须建立明确任务、角色、步骤和完成条件。

### 解决的用户问题

- 不知道在场景里要完成什么。
- 对真实交流流程缺乏预期，容易紧张或中途退出。

### 使用流程

1. 用户选择咖啡店点餐。
2. 系统展示简短任务说明和成功条件。
3. 用户开始任务。
4. 系统创建会话状态并展示 NPC 第一轮台词。

### AI 输入

- 场景模板 ID
- 用户目标上下文
- 预设难度
- 咖啡店任务步骤与完成条件

### AI 处理

- 在受控模板内生成或选择 NPC 开场台词。
- 初始化任务状态，不允许跳过必要任务步骤。

### 输出结果

```json
{
  "scene": "coffee_ordering",
  "role": "customer",
  "npc_role": "barista",
  "current_step": "choose_drink",
  "required_slots": ["drink", "size", "customization", "payment"],
  "completed_slots": []
}
```

### 规则与异常

- 咖啡店任务至少覆盖：饮品、规格、附加需求、支付交流。
- 任务说明要短，不在开始前进行大段知识教学。
- 重新开始任务时创建新会话，不污染上一次结果。

### 验收标准

1. 用户进入场景后能理解自己的角色和任务目标。
2. 每次开始任务均生成有效会话状态和 NPC 开场台词。
3. 任务状态包含全部必要步骤。
4. 用户可以退出并重新开始，且新旧会话状态相互独立。

## FR-05 NPC 互动

### 为什么需要

产品价值来自“在真实任务中使用英语”，NPC 是承载情境、提出问题和推动交流的直接交互对象。

### 解决的用户问题

- 学过表达但不知道在什么时候使用。
- 在真实环境不敢开口。

### 使用流程

1. NPC 根据当前任务步骤说出英文台词。
2. 系统提供必要的低门槛辅助。
3. 用户通过输入简短英文回答；选项不作为正常流程的主要回答方式。
4. 系统进入回答判定。
5. 回答可接受则 NPC 自然回应并推进；需要修正则进入 FR-07。

### AI 输入

- 当前任务状态
- 已完成信息槽位
- 最近若干轮对话
- NPC 角色约束
- 当前步骤允许的意图范围

### AI 处理

- 生成符合咖啡店服务员身份、当前步骤和学习难度的简短英文台词。
- 保持上下文一致，例如不重复询问已经确认的饮品。
- 不主动引入与任务无关的复杂话题。

### 输出结果

- NPC 英文台词
- 简体中文辅助内容；默认保持简短，避免替代用户理解英文台词
- 当前等待用户完成的意图

### 规则与异常

- 每轮 NPC 只提出一个主要交流目标。
- 台词应接近真实咖啡店表达，但避免不必要的俚语和复杂句。
- 模型输出不得直接改变任务完成状态；状态变化由 FR-08 的受控规则执行。

### 验收标准

1. NPC 能按饮品、规格、附加需求、支付的任务顺序完成对话。
2. NPC 不重复询问已确认信息，除非用户主动修改。
3. NPC 台词与当前步骤一致，不出现角色跳脱。
4. 连续完成标准流程时，对话可以到达任务完成状态。

## FR-06 用户回答理解与判定

### 为什么需要

初学者的表达可能不完整或有语法错误。系统需要判断“是否成功表达意图”，而不是只做严格句法匹配。

### 解决的用户问题

- 害怕因语法不完美而不敢表达。
- 不知道自己的说法在真实交流中是否能被理解。

### 使用流程

1. 用户提交简短英文文本。
2. 系统判断用户意图、关键信息和可理解程度。
3. 系统区分：可接受、可理解但需改进、无法完成当前意图、不相关。
4. 系统将判定结果交给反馈和状态推进模块。

### AI 输入

```json
{
  "current_step": "choose_size",
  "expected_intents": ["select_size"],
  "user_answer": "big one",
  "known_slots": {"drink": "latte"},
  "difficulty": "beginner"
}
```

### AI 处理

- 提取用户意图和实体信息。
- 判断表达能否让真实 NPC 理解并完成当前步骤。
- 识别影响交流的关键错误与不影响交流的次要错误。
- 输出固定枚举和结构化证据，不只返回主观评分。

### 输出结果

```json
{
  "result": "understandable_but_improvable",
  "intent": "select_size",
  "extracted_slots": {"size": "large"},
  "blocking_issue": null,
  "suggested_expression": "A large, please."
}
```

### 判定等级

| 等级 | 含义 | 系统行为 |
| --- | --- | --- |
| acceptable | 自然或足以完成交流 | 推进并可给予简短肯定 |
| understandable_but_improvable | 能被理解，但表达可优化 | 给出轻量建议后推进 |
| retry_required | 无法完成当前意图或关键信息缺失 | 解释并让用户重试 |
| off_topic | 与当前任务无关 | 提醒当前目标并提供引导 |

### 规则与异常

- 以“真实交流能否完成”为首要标准，不以语法完美为唯一标准。
- 同一输入在相同上下文中不应出现大幅不一致判定。
- AI 不确定时应倾向提供澄清或重试机会，不直接判定任务失败。

### 验收标准

1. “A latte, please.” 在选择饮品步骤被识别为可接受，并提取 latte。
2. “big one” 在已知饮品的规格步骤可被理解为 large，不因表达不标准而阻断流程。
3. 未包含必要信息的回答不会错误填充任务槽位。
4. 与点餐无关的输入不会推进任务。
5. 判定结果始终符合约定数据结构和枚举范围。

## FR-07 错误解释与修正

### 为什么需要

只告知“回答错误”无法让用户改进。反馈必须让用户知道问题在哪里、怎么说，并立刻再试一次。

### 解决的用户问题

- 不知道为什么表达没有奏效。
- 传统反馈过于抽象，无法立即迁移到真实交流。

### 使用流程

1. 系统接收 FR-06 的判定结果。
2. 对可改进回答提供一句轻量建议；不打断任务推进。
3. 对必须重试的回答解释一个最关键的问题。
4. 系统提供示例或有限提示。
5. 用户重新回答，系统再次判定。

### AI 输入

- 用户原始回答
- 判定结果和阻断原因
- 当前交流目标
- 推荐表达
- 初学者难度约束

### AI 处理

- 用简洁、易懂的方式解释影响交流的核心问题。
- 优先说明“这句话在当前场景如何表达更清楚”。
- 一次只纠正最影响任务完成的问题。

### 输出结果

- 反馈类型
- 简短解释
- 推荐表达
- 是否要求重试

### 规则与异常

- 不进行与当前任务无关的系统语法教学。
- 不因大小写、标点等不影响理解的问题阻断用户。
- 连续多次失败时逐步增强提示，最终允许用户选择示例答案继续，避免死循环。

### 验收标准

1. 反馈明确指出当前缺失或错误的关键信息。
2. 反馈包含一个当前场景可直接使用的英文表达。
3. 次要错误不阻断剧情。
4. 用户连续失败时能够获得更具体的提示，并最终继续任务。
5. 单次反馈不同时讲解多个无关知识点。

## FR-08 剧情推进与任务完成

### 为什么需要

用户需要完成一项结构明确的真实任务，而不是无限聊天。系统必须可靠管理进度和完成条件。

### 解决的用户问题

- 不知道自己是否已经具备完成任务的能力。
- 自由对话缺少明确终点和成功反馈。

### 使用流程

1. FR-06 返回回答判定和提取信息。
2. 受控状态机校验是否可以写入对应槽位。
3. 系统更新已完成步骤。
4. NPC 进入下一步骤。
5. 全部必要步骤完成后，系统结束任务。

### 实现方式

建议采用“固定任务状态机 + AI 理解和生成”的混合方案：

- 状态机负责步骤、必要信息、推进规则和完成条件。
- AI 负责理解用户表达、生成自然台词和解释反馈。
- AI 不拥有绕过规则、直接宣布成功的权限。

### 输出结果

- 更新后的任务状态
- 下一步动作
- 任务是否完成
- 本轮状态变更记录

### 完成条件

咖啡店任务至少确认：

1. 饮品
2. 规格
3. 附加需求（可明确选择“无”）
4. 支付交流

### 验收标准

1. 只有满足当前步骤要求的回答才能写入对应任务状态。
2. 四项必要信息全部完成前，系统不会宣布任务成功。
3. 用户修改已选内容时，状态能正确更新且后续对话保持一致。
4. 刷新、重复提交或模型重复输出不会造成同一步骤被异常推进多次。
5. 完成条件满足后，系统只生成一次完成事件。

## FR-09 任务结果与最小学习记录

### 为什么需要

用户需要明确感知“我完成了什么”，产品也需要记录核心行为，验证场景化学习是否有效。

### 解决的用户问题

- 学习结果不明确，难以获得成功反馈。
- 用户不知道自己哪些表达已经掌握、哪些需要改进。

### 使用流程

1. 任务完成后进入结果页。
2. 系统显示完成的现实任务。
3. 系统回顾本次使用的关键表达和最重要的改进点。
4. 用户可以重新练习或返回路线。
5. 系统保存最小任务记录。

### AI 输入

- 最终任务状态
- 用户回答及判定结果
- 本次错误与修正记录

### AI 处理

- 选择本次最有价值的少量关键表达。
- 汇总一到三个主要改进点，不生成泛化长报告。

### 输出结果

- 任务完成状态
- 已完成能力描述
- 关键表达回顾
- 主要改进点
- 重练入口

### 最小记录字段

- 会话 ID
- 目标 ID
- 场景 ID
- 开始与完成时间
- 是否完成
- 各步骤尝试次数
- 判定等级分布
- 使用提示次数
- 错误类型摘要

### 验收标准

1. 完成任务后，用户能看到“已完成咖啡店点餐”这一结果，而非仅看到抽象分数。
2. 结果页包含本次真实使用过的关键表达。
3. 未完成任务不会被记录为完成。
4. 用户可从结果页重新开始任务或返回路线。
5. 最小学习记录能支持计算首次场景完成率和场景任务完成率。

## FR-10 AI 异常降级

### 为什么需要

模型可能超时、返回错误格式、生成不合适内容或出现上下文不一致。如果没有降级策略，核心学习任务会被中断。

### 解决的用户问题

- 学习过程中因系统不稳定而无法继续。
- AI 输出异常导致困惑或错误学习。

### 实现方式

- 对结构化输出进行 schema 校验。
- 对可重试错误执行有限次数自动重试。
- 对关键节点准备预置路线、NPC 台词和示例反馈。
- 状态机仅接受校验通过的状态变更。
- 记录异常类型，供质量评估。

### 验收标准

1. AI 超时或返回非法 JSON 时，界面不展示技术错误和原始输出。
2. 路线生成失败时，用户仍能进入预置咖啡店任务。
3. NPC 台词生成失败时，系统可使用当前步骤的预置台词继续。
4. 异常输出不能越权修改任务状态。
5. 每次降级均记录错误类型和发生节点。

## 5. 核心数据与状态

### 5.1 Goal（学习目标）

- goal_id
- raw_goal
- goal_type
- destination
- departure_time（可空）
- scope_status
- confirmed_at

### 5.2 LearningRoute（学习路线）

- route_id
- goal_id
- scene_items
- generated_at
- generation_source（AI / fallback）

### 5.3 SceneSession（场景会话）

- session_id
- user_or_device_id
- goal_id
- scene_id
- current_step
- slot_values
- status（not_started / in_progress / completed / abandoned）
- started_at
- completed_at

### 5.4 Turn（互动轮次）

- turn_id
- session_id
- npc_message
- user_answer
- evaluation_result
- extracted_slots
- feedback
- retry_count
- created_at

## 6. 关键埋点与首轮指标

### 6.1 主漏斗事件

1. `goal_input_submitted`
2. `goal_confirmed`
3. `route_generated`
4. `scene_started`
5. `scene_step_completed`
6. `hint_used`
7. `scene_completed`
8. `scene_abandoned`
9. `scene_restarted`

### 6.2 产品指标定义草案

- 首次场景完成率：首次开始咖啡店任务的用户中，完成任务的用户占比。
- 场景任务完成率：已开始的场景会话中，完成会话的占比。
- 独立完成率：未使用示例答案且完成任务的会话数 / 已完成会话数。
- 辅助完成率：使用过示例答案并完成任务的会话数 / 已完成会话数。
- 中位完成时长：从 `scene_started` 到 `scene_completed` 的中位时长。
- 步骤阻塞率：某一步骤发生两次及以上 `retry_required` 的会话数 / 到达该步骤的会话数。
- 7 日留存：首次使用后的第 7 个自然日再次产生有效学习行为的用户占比。单场景 MVP 阶段仅作为观察指标，不作为产品成败判断依据。

### 6.3 AI 质量指标草案

- 结构化输出成功率
- 降级触发率
- 人工抽检的场景真实性合格率
- 回答判定准确率
- 难度匹配满意度

AI 指标的样本集和评分量表见第 13 节；目标阈值在技术验证后冻结。

## 7. 已确认的产品约束

以下规则于 2026-08-22 确认，后续页面、技术和 AI 方案均以此为准：

1. **首发终端**：浏览器网页端，不开发原生 App。
2. **界面语言**：导航、任务说明、错误解释和结果反馈使用简体中文；训练对话主体使用英文。
3. **互动方式**：以“NPC 提问—用户输入简短英文—AI 判断与回应”的问答形式为主。选项不作为主流程答案，只能在连续失败后的辅助机制中出现。
4. **生成边界**：采用“受控场景模板和状态机 + AI 个性化理解、表达和反馈”的混合方案，不采用完全自由生成。
5. **路线范围**：路线允许展示机场、酒店、购物等场景，但除咖啡店点餐外均标记为“暂未开放”。
6. **用户身份**：MVP 不强制登录。系统创建匿名用户 ID，在服务端保存最小学习与埋点记录。
7. **成功口径**：使用提示后完成仍算任务完成，但记录为“辅助完成”；未使用示例答案完成记为“独立完成”。
8. **指标口径**：单场景 MVP 优先验证完成率、独立完成率、辅助完成率和阻塞步骤；7 日留存仅作观察。

## 8. 信息架构与核心页面

### 8.1 页面清单

| 页面编号 | 页面 | 用户目标 | 主要内容 | 主要出口 |
| --- | --- | --- | --- | --- |
| P-01 | 目标输入页 | 说明这次出境旅行需求 | 产品价值说明、目标输入框、示例 | 目标确认页 |
| P-02 | 目标确认页 | 确认 AI 是否理解正确 | 原始目标、结构化摘要、修改与确认 | 返回修改、生成路线 |
| P-03 | 学习路线页 | 知道应该完成哪些现实任务 | 路线、场景能力描述、开放状态 | 进入咖啡店任务 |
| P-04 | 任务说明页 | 理解角色、目标和完成条件 | 场景背景、角色、四项任务目标 | 开始任务、返回路线 |
| P-05 | NPC 问答页 | 通过英语问答完成点餐 | NPC 台词、中文辅助、输入框、反馈、进度 | 继续、退出任务 |
| P-06 | 任务结果页 | 确认完成能力并回顾表达 | 完成类型、能力结果、关键表达、改进点 | 重新练习、返回路线 |

### 8.2 页面流转

```text
P-01 目标输入
  ├─ 输入无效 → 原页提示
  └─ 输入有效 → P-02 目标确认
                    ├─ 修改 → P-01
                    └─ 确认 → P-03 学习路线
                                  ├─ 点击未开放场景 → 原页说明
                                  └─ 咖啡店 → P-04 任务说明
                                                  ├─ 返回 → P-03
                                                  └─ 开始 → P-05 NPC 问答
                                                               ├─ 中途退出 → P-03，记录 abandoned
                                                               ├─ 需要重试 → P-05 原步骤
                                                               └─ 完成 → P-06 任务结果
                                                                           ├─ 重练 → P-04
                                                                           └─ 返回 → P-03
```

### 8.3 P-05 NPC 问答页交互规则

- 页面中心始终突出当前 NPC 英文问题。
- 中文辅助解释次级展示，不先给出完整标准答案。
- 用户通过单行或自适应高度文本框输入简短英文。
- 提交后立即显示“正在理解”，防止重复提交。
- `acceptable`：显示简短肯定并自然进入下一问。
- `understandable_but_improvable`：显示一句优化建议，同时推进。
- `retry_required`：保留当前步骤，解释一个关键问题，允许重新回答。
- `off_topic`：提示当前任务并给出方向，不推进状态。
- 同一步骤连续两次 `retry_required` 后显示更明确的句型提示。
- 同一步骤连续三次 `retry_required` 后允许用户点击示例答案继续；一旦使用，本次会话记为“辅助完成”。
- 关闭或刷新页面后，系统应尽可能恢复到最近一次已确认轮次，不重复写入已完成步骤。

### 8.4 页面级验收标准

1. 用户可从目标输入连续完成咖啡店任务，主链路不存在必须登录的阻断点。
2. 所有产品导航和学习解释均为简体中文，NPC 对话和用户训练输入为英文。
3. 未开放场景具有清晰状态，点击后不会进入空白页或伪造任务。
4. 每个页面只有一个视觉上的主要行动按钮。
5. 在 NPC 问答页刷新后，不丢失已经确认的饮品、规格等任务信息。

## 9. 咖啡店任务状态机

### 9.1 状态定义

| 状态 | NPC 目标 | 必须获得的信息 | 完成后默认下一状态 |
| --- | --- | --- | --- |
| `not_started` | 等待用户开始 | 无 | `choose_drink` |
| `choose_drink` | 询问饮品 | `drink` | 下一个未满足状态 |
| `choose_size` | 询问规格 | `size` | 下一个未满足状态 |
| `choose_customization` | 询问糖、奶等需求 | `customization` 或明确的 `none` | 下一个未满足状态 |
| `payment` | 告知金额并询问/确认支付 | `payment_acknowledged`，可包含 `payment_method` | `completed` |
| `completed` | 确认订单完成 | 全部必要信息 | 终态 |
| `abandoned` | 用户主动退出或会话失效 | 无 | 终态，可重新开始新会话 |

### 9.2 状态推进规则

1. 系统以“下一个未满足的必要信息”决定下一状态，不仅依赖固定轮次编号。
2. 用户可以在一句话中提供多个信息，例如 “A large latte with oat milk, please.”；AI 可同时提取 `drink`、`size` 和 `customization`。
3. 多信息提取通过 schema 和允许值校验后写入状态；系统跳过已满足步骤，保持对话自然。
4. `retry_required` 和 `off_topic` 不写入任务信息，不推进状态。
5. `understandable_but_improvable` 可以写入信息并推进状态。
6. 用户明确修改已确认内容时覆盖对应值，并记录修改事件。
7. AI 只能建议状态变化；后端状态机负责最终校验和写入。
8. 每轮写入使用幂等键，重复请求不能重复推进。

### 9.3 完成类型判定

```text
全部必要信息已满足
  ├─ 本次未使用示例答案 → independently_completed
  └─ 本次使用过示例答案 → assisted_completed
```

普通中文解释、句型提示和纠错建议不直接改变完成类型；只有用户点击并采用系统提供的完整示例答案时，记为辅助完成。

### 9.4 状态机验收用例

1. 用户分四轮分别提供饮品、规格、定制和支付信息，可正常完成。
2. 用户首轮输入包含饮品、规格和定制信息时，系统正确填充三项并进入支付步骤。
3. 用户先选择 large 后改为 small，最终订单只保留 small。
4. 用户连续输入无关内容时，会话不会错误完成。
5. 同一提交请求重复到达两次，状态只推进一次。
6. 未确认支付前，即使模型输出“订单完成”，后端也不得进入 `completed`。

## 10. AI 系统设计

### 10.1 设计原则

- **任务可靠性优先于生成自由度**：AI 不控制任务完成条件。
- **交流成功优先于语法完美**：先判断能否完成真实交流，再判断表达是否可优化。
- **结构化输出优先**：所有会影响产品状态的 AI 结果必须通过 schema 校验。
- **一次解决一个问题**：面向初学者的反馈保持简短、可立即使用。
- **最少上下文**：只传递完成当前判断所需的历史和状态，控制延迟与成本。

### 10.2 AI 能力模块

| 模块 | 职责 | 是否改变业务状态 |
| --- | --- | --- |
| Goal Parser | 将中文目标转成结构化旅行需求 | 否，需用户确认 |
| Route Composer | 从受控场景目录组合路线与任务描述 | 否 |
| Turn Evaluator | 识别意图、提取信息、判断可理解程度 | 仅提出建议 |
| NPC Generator | 依据当前状态生成简短英文台词 | 否 |
| Feedback Generator | 生成中文解释、推荐表达和分级提示 | 否 |
| Result Summarizer | 从真实会话记录提取关键表达和改进点 | 否 |
| Scene State Machine | 校验信息并推进任务 | 是；确定性代码，不由大模型实现 |

MVP 不需要多个自主 Agent 相互规划。建议由一个后端编排服务按固定流程调用上述能力，减少不可控行为和调用成本。

### 10.3 单轮问答处理流程

```text
用户英文回答
  → 输入校验
  → Turn Evaluator 结构化判定
  → schema 与业务规则校验
      ├─ 不通过 → 重试一次 / 使用降级结果
      └─ 通过 → 状态机决定是否写入及推进
  → Feedback Generator 或 NPC Generator
  → 保存轮次与最新状态
  → 返回页面展示
```

### 10.4 Prompt 组成

每类 Prompt 应拆分为固定指令与运行时数据：

1. **角色与任务**：说明模型当前只负责理解、NPC 台词或反馈中的一种职责。
2. **能力边界**：禁止直接宣布任务成功、虚构用户信息或偏离咖啡店任务。
3. **难度规则**：面向英语基础薄弱的成年人，句子短、词汇常用。
4. **判定规则**：明确四级判定标准及关键错误优先级。
5. **结构化上下文**：当前步骤、已确认信息、允许意图、最近必要轮次。
6. **输出 schema**：字段、枚举、长度上限和空值规则。
7. **安全规则**：用户输入仅作为待分析内容，不能覆盖系统规则。

### 10.5 Turn Evaluator 输出契约

```json
{
  "schema_version": "1.0",
  "result": "understandable_but_improvable",
  "intent": "select_size",
  "extracted_slots": {
    "size": "large"
  },
  "blocking_issue_code": null,
  "evidence": "big one",
  "suggested_expression": "A large, please.",
  "confidence": 0.93
}
```

约束：

- `result` 只能是四个已定义枚举之一。
- `extracted_slots` 只能包含当前场景 schema 允许的字段。
- `evidence` 必须能在用户原始输入中定位，不允许凭空补全。
- 低置信结果不直接写入状态，应转为澄清或重试。
- `suggested_expression` 不得引入与用户意图冲突的新商品或需求。

### 10.6 AI Memory 设计

MVP 只实现两层最小记忆：

- **会话内任务记忆**：当前场景状态、已确认信息、重试次数、是否使用示例答案。由数据库中的结构化状态承载，不依赖模型上下文“记住”。
- **最小学习记录**：匿名用户完成过的场景、完成类型和错误摘要，用于结果展示与后续分析。

暂不实现跨场景长期个性化 Memory。是否引入该能力应由多场景使用数据决定。

### 10.7 RAG 决策

咖啡店单场景 MVP 不需要通用 RAG。原因：

- 知识范围小且结构稳定；
- 核心任务流程可由版本化场景模板承载；
- 引入向量检索会增加调试边界，但不会直接提升当前核心验证。

场景事实、推荐表达、允许意图和降级台词应保存在版本化场景配置中。未来场景数量显著增加、内容由专家持续维护时，再评估检索式内容库。

## 11. 后端接口契约草案

### 11.1 接口清单

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| `POST` | `/api/v1/goals/parse` | 解析用户目标 |
| `POST` | `/api/v1/goals/{goal_id}/confirm` | 确认或修正目标 |
| `POST` | `/api/v1/routes` | 生成学习路线 |
| `POST` | `/api/v1/scene-sessions` | 创建场景会话 |
| `GET` | `/api/v1/scene-sessions/{session_id}` | 恢复会话状态 |
| `POST` | `/api/v1/scene-sessions/{session_id}/turns` | 提交一轮用户回答 |
| `POST` | `/api/v1/scene-sessions/{session_id}/abandon` | 主动退出会话 |
| `GET` | `/api/v1/scene-sessions/{session_id}/result` | 获取任务结果 |

### 11.2 创建场景会话

请求：

```json
{
  "scene_id": "coffee_ordering",
  "goal_id": "goal_123",
  "scene_version": "1.0"
}
```

响应：

```json
{
  "session_id": "session_123",
  "status": "in_progress",
  "current_step": "choose_drink",
  "npc_message": "Hi! What can I get for you today?",
  "chinese_support": "店员正在询问你想点什么。",
  "progress": {"completed": 0, "total": 4}
}
```

### 11.3 提交一轮回答

请求：

```json
{
  "answer": "A large latte with oat milk, please.",
  "idempotency_key": "client-generated-uuid",
  "client_state_version": 3
}
```

响应：

```json
{
  "turn_id": "turn_456",
  "evaluation": {
    "result": "acceptable",
    "suggested_expression": null
  },
  "feedback": null,
  "accepted_slots": {
    "drink": "latte",
    "size": "large",
    "customization": "oat_milk"
  },
  "session": {
    "state_version": 4,
    "current_step": "payment",
    "completion_type": null
  },
  "npc_message": "Great. That'll be $5.50. How would you like to pay?",
  "chinese_support": "店员确认了订单，并询问你的支付方式。"
}
```

### 11.4 通用错误响应

```json
{
  "error": {
    "code": "AI_TEMPORARILY_UNAVAILABLE",
    "message": "暂时没能理解，请再试一次。",
    "retryable": true,
    "request_id": "req_123"
  }
}
```

规则：

- 面向用户的 `message` 使用简体中文且不暴露模型、Prompt 或堆栈信息。
- 状态版本不一致时返回冲突错误，并让客户端重新拉取最新状态。
- 同一 `idempotency_key` 重复提交时返回首次处理结果。
- 接口不接受客户端直接设置 `completed` 或任务槽位。

## 12. 非功能需求

### 12.1 性能与体验

- 用户提交回答后 300 毫秒内显示明确的处理中状态。
- 普通非 AI 接口的服务端处理时间目标为 P95 不超过 1 秒。
- 单轮 AI 问答从提交到可展示结果的目标为 P95 不超过 8 秒；超过 10 秒应提示仍在处理中或触发可恢复降级。
- 防止按钮重复点击和并发提交导致重复轮次。
- 网页应适配常见手机与桌面浏览器宽度，最小支持 375 CSS 像素宽度。

### 12.2 可靠性

- 每轮通过业务校验后立即持久化任务状态和轮次。
- 页面刷新、短暂断网或请求重试不能造成已完成信息丢失或重复推进。
- 关键 AI 能力必须有 schema 校验、有限重试和预置降级内容。
- 场景配置带版本号；正在进行的会话固定使用创建时版本。

### 12.3 内容质量与安全

- NPC 不生成歧视、色情、暴力或与学习任务无关的危险内容。
- 用户输入中的指令不得改变系统角色、完成条件或输出格式。
- 所有会影响学习结果的模型输出必须经过业务规则校验。
- 场景标准表达和降级内容上线前需完成人工语言审核。
- 不向用户展示未经校验的原始模型响应。

### 12.4 隐私与数据最小化

- MVP 使用随机匿名用户 ID，不要求姓名、手机号或精确位置。
- 只保存实现任务恢复、结果计算、质量评估和核心指标所必需的数据。
- 日志中避免记录浏览器指纹等不必要标识。
- 用户文本进入模型服务前，不主动拼接未声明的个人信息。
- 数据保留周期、删除机制和模型供应商的数据使用条款需在上线评审前确认。

### 12.5 兼容性与可用性

- 支持最新版及前一个主要版本的 Chrome、Safari 和 Edge。
- 键盘可完成输入、提交和主要页面导航。
- 反馈不能只依赖颜色表达成功或错误状态。
- 中文解释和按钮避免使用专业英语教学术语。

### 12.6 成本约束

- 按任务步骤裁剪上下文，只携带最近必要轮次和结构化状态。
- 可由确定性代码完成的校验、排序和状态推进不得调用大模型。
- 对固定开场、异常降级等内容优先使用已审核模板。
- 记录每个场景会话的模型调用次数、输入输出 token 和估算成本。
- 单次完成任务的目标成本需在模型选型与压力测试后确定。

## 13. AI 评测与验收方案

### 13.1 离线评测集

建立版本化测试集，至少覆盖：

- 目标解析：明确目标、信息缺失、非旅游目标、模糊表达。
- 回答判定：标准表达、非标准但可理解表达、关键信息缺失、错答、无关输入。
- 信息提取：单一信息、多信息合并表达、修改既有信息、否定表达。
- 反馈质量：关键问题识别、中文解释清晰度、推荐表达一致性。
- NPC 质量：角色一致、上下文一致、难度适配、任务相关性。
- 安全与鲁棒性：提示注入、超长输入、乱码、要求跳过任务或伪造成功。

### 13.2 人工标注字段

每条评测样本至少包含：

- 预期判定等级
- 预期意图
- 允许提取的信息
- 禁止推断的信息
- 是否允许推进
- 推荐表达要点
- 关键失败严重度

### 13.3 上线前质量门槛草案

| 指标 | 建议门槛 | 说明 |
| --- | --- | --- |
| 结构化输出有效率 | ≥ 99% | 包含自动重试后的最终结果 |
| 判定等级人工一致率 | ≥ 90% | 与双人复核后的标准答案比较 |
| 关键槽位提取准确率 | ≥ 95% | 不允许凭空补全饮品、规格等信息 |
| 错误推进率 | ≤ 2% | 不满足条件却推进任务，属于高风险错误 |
| NPC 角色与任务相关性 | ≥ 95% | 人工抽检合格率 |
| 降级后任务可继续率 | 100% | 针对已设计的模型失败场景 |

具体阈值需要在首个模型原型和评测集跑通后复核，但“错误推进率”应作为优先级最高的质量门槛。

### 13.4 端到端验收场景

1. **标准独立完成**：用户四轮给出自然表达，结果为 `independently_completed`。
2. **非标准但可理解**：用户使用 “big one”等表达，得到优化建议但不被阻断。
3. **多信息表达**：用户一句提供多个订单信息，系统自然跳到下一个未满足步骤。
4. **反复失败**：提示逐步增强，第三次后可用示例答案完成，结果为 `assisted_completed`。
5. **修改订单**：中途修改规格，NPC 和最终状态保持一致。
6. **模型异常**：目标、NPC 或反馈生成失败时，预置内容保证主链路可继续。
7. **刷新恢复**：任务中刷新网页，恢复至最近已确认状态，不重复计数。
8. **越权输入**：用户要求系统直接标记成功，状态机拒绝并继续当前任务。

## 14. 尚待确认的问题

以下事项不阻塞当前功能设计，但进入视觉设计或上线评审前需要确认：

1. 网页首版是优先适配手机浏览器，还是桌面与手机同等优先？
2. 匿名学习记录的具体保留周期和用户主动清除入口。
3. AI 模型供应商、单次任务成本目标和可接受的响应时间最终阈值。
4. 是否在结果页加入一个非强制的“现在独立点餐有多大信心”单题反馈，用于衡量用户主观能力提升。

## 15. 下一步任务

1. 确认第 14 节中影响视觉设计和上线策略的事项。
2. 输出六个核心页面的低保真交互原型与页面状态清单。
3. 将咖啡店任务模板细化为可执行的场景配置和完整测试语料。
4. 制作 Prompt v0.1，并用离线评测集验证目标解析、回答判定和反馈质量。
5. 基于接口草案完成前后端技术选型、数据库结构和部署方案。
6. 补充风险、里程碑与发布验收清单，形成 PRD v1.0。
