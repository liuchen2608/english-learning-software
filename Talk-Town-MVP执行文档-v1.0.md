# Talk Town MVP 执行文档

> 版本：v1.0  
> 状态：已被《Talk Town 完整 AI 产品 PRD v1.0》替代，仅保留验证方案历史  
> 目标阶段：咖啡店文字任务学习效果验证  
> 更新日期：2026-08-22

## 1. MVP 一句话定义

为英语基础薄弱、近期准备出境旅游的成年人提供一次网页端咖啡店点餐训练，并通过“训练前无提示任务—AI 引导训练—训练后无提示迁移任务”判断用户是否真正获得了可迁移的文字交流能力。

## 2. MVP 要回答的问题

### 核心问题

一次 AI 引导的场景任务训练，能否让目标用户在没有答案提示的情况下，比训练前独立完成更多咖啡店英语交流步骤？

### 辅助问题

- 用户能否理解并完成问答式训练？
- AI 能否正确理解不标准但可交流的英文表达？
- 中文纠错和分级提示是否能帮助用户继续任务？
- 用户完成训练后是否感到更有信心？

### 本期不能回答

- 用户能否在真实环境中开口交流；
- 用户的发音是否可理解；
- 学习效果能否长期保持；
- 多场景学习路线是否提高留存；
- 订阅商业模式是否成立。

## 3. 目标用户

有效 MVP 测试用户应同时满足：

1. 年满 18 周岁；
2. 未来 6 个月内计划或强烈考虑出境旅游；
3. 自评无法独立、流畅地完成英文咖啡店点餐；
4. 能识别英文字母和少量常见英文词；
5. 能输入简单英文单词或短句；
6. 日常不以英语作为主要工作或生活语言。

完全零英文识字能力、已经能熟练完成旅行交流、以考试或留学为主要目标的用户不计入本轮有效样本。

## 4. 产品形态

- 响应式网页，默认手机优先，同时兼容桌面浏览器；
- 界面、任务说明和反馈使用简体中文；
- NPC 台词和用户训练输入以英文为主；
- 用户无需注册，系统生成匿名用户 ID；
- 不开发原生 App；
- 不包含语音输入和发音评分。

## 5. MVP 完整流程

```text
欢迎与项目说明
  → 用户资格筛选
  → 训练前信心评分
  → 任务说明
  → 基线任务 A（无提示）
  → AI 引导训练 B（纠错与分级提示）
  → 迁移任务 C（无提示）
  → 训练后信心评分
  → 结果页
```

### 5.1 三段任务

| 任务 | 目的 | AI 是否判定 | 是否展示纠错 | 是否提供答案 |
| --- | --- | --- | --- | --- |
| 基线任务 A | 测量训练前能力 | 是，仅后台记录 | 否 | 否 |
| 引导训练 B | 完成学习与修正 | 是，向用户反馈 | 是 | 连续失败后允许 |
| 迁移任务 C | 测量训练后能力 | 是，仅后台记录 | 否 | 否 |

A、B、C 都覆盖饮品、规格、定制需求和支付，但必须使用不同商品组合及不同 NPC 表达，防止用户只记忆答案。

## 6. 页面清单

| 编号 | 页面 | 核心内容 | 主操作 |
| --- | --- | --- | --- |
| P-01 | 欢迎页 | MVP 价值、预计用时、不是考试的说明 | 开始体验 |
| P-02 | 用户筛选页 | 出境意愿、英语使用能力等筛选问题 | 提交筛选 |
| P-03 | 训练前信心页 | 1–5 分独立点餐信心 | 开始任务 |
| P-04 | 任务说明页 | 用户角色、四项任务、交互说明 | 开始基线任务 |
| P-05 | 基线任务页 | NPC 英文提问、英文输入框 | 提交回答 |
| P-06 | 引导训练说明页 | 告知接下来会得到 AI 帮助 | 开始训练 |
| P-07 | AI 引导训练页 | NPC、英文输入、中文反馈、任务进度 | 提交/重试 |
| P-08 | 迁移任务说明页 | 告知需要独立完成新任务 | 开始测试 |
| P-09 | 迁移任务页 | NPC 英文提问、英文输入框 | 提交回答 |
| P-10 | 训练后信心页 | 1–5 分独立点餐信心 | 查看结果 |
| P-11 | 结果页 | 前后表现、已掌握意图、关键表达 | 重新训练 |

## 7. P0 功能清单

| 编号 | 功能 | 必须实现的结果 |
| --- | --- | --- |
| MVP-01 | 匿名实验会话 | 同一用户的三段任务和评分可关联 |
| MVP-02 | 用户资格筛选 | 能区分有效样本、无效样本和内部流量 |
| MVP-03 | 训练前后信心评分 | 记录 1–5 分并计算变化 |
| MVP-04 | 基线任务 | 无反馈条件下产生 0–4 分基线成绩 |
| MVP-05 | AI 引导训练 | 完成问答、纠错、提示和任务推进 |
| MVP-06 | 迁移任务 | 在无提示新内容中产生 0–4 分训练后成绩 |
| MVP-07 | 任务状态机 | 可靠管理四项任务意图和完成条件 |
| MVP-08 | AI 回答判定 | 输出固定等级、意图、槽位和建议表达 |
| MVP-09 | AI 异常降级 | 模型错误时任务仍能继续或安全恢复 |
| MVP-10 | 结果展示 | 展示训练前后差异，不夸大为口语能力 |
| MVP-11 | 埋点与数据导出 | 能复算所有核心产品与 AI 指标 |
| MVP-12 | 数据清除 | 用户可清除当前匿名身份关联记录 |

## 8. 核心任务定义

### 8.1 必要交流意图

每个任务都包含四个必要意图：

| 步骤 | 用户需要完成的事情 | 示例，不作为唯一答案 |
| --- | --- | --- |
| 饮品 | 表达想要的饮品 | `A latte, please.` |
| 规格 | 表达杯型或大小 | `A large, please.` |
| 定制 | 表达糖、奶等需求，或明确无需求 | `With oat milk, please.` |
| 支付 | 回应支付环节 | `I'll pay by card.` |

### 8.2 状态机

```text
not_started
  → choose_drink
  → choose_size
  → choose_customization
  → payment
  → completed
```

状态机按“下一个未满足的必要意图”推进。用户可以一次提供多个信息，例如：

`A large latte with oat milk, please.`

如果 AI 正确提取饮品、规格和定制信息，状态机应直接进入支付步骤。

### 8.3 状态规则

- AI 只提出判定和槽位建议，后端状态机负责最终写入；
- 无关回答和必须重试的回答不能推进；
- 可理解但不够自然的表达可以计为成功并推进；
- 用户明确修改订单时覆盖旧值；
- 未完成全部必要意图前不能进入完成状态；
- 每轮使用幂等键，重复提交只处理一次；
- 刷新网页后恢复最近一次已经确认的状态。

## 9. AI 回答判定

### 9.1 判定等级

| 判定 | 含义 | 引导训练行为 | 基线/迁移计分 |
| --- | --- | --- | --- |
| `acceptable` | 自然或足以完成交流 | 肯定并推进 | 1 分 |
| `understandable_but_improvable` | 可以理解但能优化 | 轻量建议并推进 | 1 分 |
| `retry_required` | 关键信息缺失或无法完成意图 | 解释并重试 | 0 分 |
| `off_topic` | 与当前任务无关 | 拉回任务，不推进 | 0 分 |

### 9.2 结构化输出

```json
{
  "schema_version": "1.0",
  "result": "understandable_but_improvable",
  "intent": "select_size",
  "extracted_slots": {
    "size": "large"
  },
  "blocking_issue_code": null,
  "evidence": "big one",
  "suggested_expression": "A large, please.",
  "confidence": 0.93
}
```

### 9.3 强制规则

- 不因大小写、标点或不影响理解的次要语法问题阻断用户；
- 不得推断用户没有表达的饮品、规格或定制需求；
- `evidence` 必须能在用户原始输入中找到；
- 低置信结果不直接写入状态，应要求澄清或重试；
- 推荐表达不得改变用户原本想点的内容；
- 用户要求“直接算我成功”不能改变任务状态。

## 10. 引导与纠错规则

### 第一次失败

- 说明当前缺少的一个关键信息；
- 给出中文解释；
- 不直接展示完整答案。

### 第二次连续失败

- 提供更明确的英文句型框架；
- 仍由用户填写关键信息。

### 第三次连续失败

- 允许用户查看并使用完整示例答案；
- 记录 `example_answer_used=true`；
- 本次引导训练标记为辅助完成。

### 反馈限制

- 一次只纠正最影响交流的问题；
- 不展开与当前任务无关的语法课程；
- 中文反馈保持简短；
- 推荐表达必须适合英语基础薄弱的成年人。

## 11. 基线和迁移计分

### 11.1 评分

每个必要意图 1 分，总分 0–4：

```text
pre_score  = 基线任务成功意图数
post_score = 迁移任务成功意图数
score_gain = post_score - pre_score
```

### 11.2 数据有效性

以下情况不直接按用户失败计分：

- 模型超时且降级无法继续；
- 页面或网络错误造成回答未保存；
- 场景版本或评分版本缺失；
- 内部测试流量；
- 用户不符合目标样本标准。

上述会话应标记原因，并与有效样本分开统计。

## 12. 结果页规则

结果页必须展示：

- 训练前完成了几个步骤；
- 训练后完成了几个步骤；
- 哪些交流意图已经可以独立表达；
- 引导训练中最有价值的少量关键表达；
- 训练前后信心评分变化；
- 重新训练入口。

结果页不得：

- 用一个无法解释的综合分数代替具体能力；
- 宣称用户已经掌握英语口语；
- 将使用完整示例答案完成包装成独立完成；
- 展示未经审核的模型原始分析。

## 13. 数据与埋点

### 13.1 最小数据字段

- `anonymous_user_id`
- `experiment_session_id`
- `eligibility_result`
- `pre_confidence` / `post_confidence`
- `pre_score` / `post_score` / `score_gain`
- `guided_completion_type`
- `example_answer_used`
- `task_variant_versions`
- `prompt_version` / `scoring_version`
- `AI_error_count` / `fallback_count`
- `started_at` / `completed_at`

### 13.2 核心事件

1. `eligibility_completed`
2. `pre_confidence_submitted`
3. `baseline_started`
4. `baseline_step_evaluated`
5. `baseline_completed`
6. `guided_training_started`
7. `guided_hint_used`
8. `guided_example_used`
9. `guided_training_completed`
10. `transfer_started`
11. `transfer_step_evaluated`
12. `transfer_completed`
13. `post_confidence_submitted`
14. `result_viewed`
15. `experiment_abandoned`

## 14. 成功标准

### 14.1 产品指标

| 指标 | MVP 门槛 |
| --- | --- |
| 平均能力增量 | ≥ 1.0 个步骤 |
| 个人改善率 | ≥ 60% |
| 迁移任务完整完成率 | ≥ 50% |
| 引导训练完成率 | ≥ 80% |
| 过度辅助率 | ≤ 40% |
| 信心改善率 | ≥ 60% |

### 14.2 AI 与体验护栏

| 指标 | MVP 门槛 |
| --- | --- |
| 关键槽位提取准确率 | ≥ 95% |
| 错误推进率 | ≤ 2% |
| 结构化输出有效率 | ≥ 99%（含有限重试） |
| 已设计异常的降级可继续率 | 100% |
| 单轮 AI 响应时间 | P95 ≤ 8 秒 |
| 严重内容安全事件 | 0 |

### 14.3 验证规模

- 开发完成后先进行 5 名目标用户的可用性测试；
- 修复系统性问题并冻结任务、Prompt 和评分版本；
- 再获得至少 30 个有效目标用户样本；
- 30 人验证开始后不为了达到门槛修改评分口径。

## 15. 非功能要求

### 性能

- 提交后 300 毫秒内显示处理中状态；
- 普通接口 P95 不超过 1 秒；
- AI 单轮响应 P95 不超过 8 秒；
- 超时必须允许重试或进入预置降级。

### 可靠性

- 每个有效轮次完成后立即保存；
- 刷新、断网重试或重复点击不能重复推进；
- 正在进行的会话固定使用创建时的场景版本；
- 模型非法输出不能直接展示或写入状态。

### 安全与隐私

- 不强制收集姓名、手机号和精确位置；
- 原始对话和实验记录建议保留 90 天；
- 提供清除当前匿名身份记录的入口；
- 上线测试前确认模型供应商的数据使用条款；
- 严重内容安全事件为零容忍发布阻断项。

### 兼容性

- 支持最新版及前一个主要版本的 Chrome、Safari 和 Edge；
- 最小适配宽度为 375 CSS 像素；
- 键盘能够完成输入、提交和主要导航；
- 错误和成功状态不能只依赖颜色表达。

## 16. 不进入 MVP 的功能

- 用户学习目标解析；
- AI 个性化路线生成；
- 机场、酒店、购物等其他场景；
- 用户账户和跨设备同步；
- 语音聊天、语音识别和发音评分；
- 长期 Memory 和动态难度；
- RAG 和多 Agent；
- 社交、商城、积分和订阅付费；
- 7 日留存优化。

## 17. MVP Definition of Done

只有同时满足以下条件，MVP 才视为开发完成并可进入 5 人测试：

1. P-01 至 P-11 主链路可在手机和桌面网页完成；
2. A/B/C 三套场景通过产品、英语内容和技术评审；
3. 基线与迁移任务使用同一评分规则且不泄露答案；
4. AI 引导训练的四级判定、纠错和三级提示可工作；
5. 状态机、多信息输入、修改订单、重复提交和刷新恢复通过测试；
6. AI 结构化输出、超时、非法输出和降级通过测试；
7. 埋点和数据导出能够复算核心指标；
8. 内部测试流量和无效样本可以排除；
9. 没有阻断缺陷和严重内容安全问题；
10. 匿名数据说明、保留周期和清除机制已经确认。

## 18. 验证后的决策

### 进入产品化

核心学习效果指标和 AI 护栏达标，并且没有系统性可用性问题。下一阶段可以恢复目标理解、学习路线和第二个真实场景规划。

### 原范围迭代

用户表现有改善趋势，但引导完成率、AI 判定或响应速度未达标。继续优化咖啡店任务，不增加场景。

### 重新审视

用户能完成引导训练，但迁移任务没有改善；或改善主要来自记忆完整答案。此时停止范围扩张，重新评估训练机制和核心价值。

## 19. 开发前待确认

1. 正式确认目标理解和路线生成不进入本期 MVP；
2. 确认手机优先的响应式网页方案；
3. 确认成功指标及门槛作为首轮决策基线；
4. 确认匿名原始数据保留 90 天及用户清除方式；
5. 指定产品、技术、英语内容和用户测试负责人；
6. 完成 A/B/C 三套场景内容后冻结任务难度。

## 20. 关联文档

- 《Talk Town 产品需求文档 v0.3》
- 《Talk Town AI 功能与技术规格 v0.2》
- 《Talk Town MVP 项目立项书 v1.0》
