# Talk Town 产品需求文档（PRD）

> 版本：v0.3（MVP 验证版）  
> 状态：已被《Talk Town 完整 AI 产品 PRD v1.0》替代，仅保留决策历史  
> 更新日期：2026-08-22

## 1. 文档目的

本文档定义 Talk Town 首轮 MVP 要验证的产品假设、目标用户、范围、用户故事、成功标准和发布决策。AI 行为、场景状态机、接口与非功能要求参见《Talk Town AI 功能与技术规格 v0.2》。

本轮目标不是证明 Talk Town 已经能够全面提升英语口语，而是用最小范围回答一个更具体的问题：

> 一次 AI 引导的咖啡店英语任务训练，能否提升目标用户在无提示条件下完成同类文字交流任务的能力？

## 2. 产品定位

Talk Town 是一款 AI 场景化英语学习产品，帮助近期准备出境旅游、英语基础薄弱的成年人，通过完成真实任务，学习可以立即使用的英语表达。

### 2.1 核心价值

用户得到的结果不是“学完一节英语课”，而是“比训练前更有能力完成一次具体的英语交流任务”。

### 2.2 当前 MVP 能验证的边界

首版使用网页文字问答，因此可以验证：

- 用户是否理解咖啡店点餐的交流流程；
- 用户是否能够用简短英文表达饮品、规格、定制和支付意图；
- AI 反馈是否能帮助用户修正表达；
- 训练后，用户能否在无示例答案的同类任务中表现得更好。

首版不能验证：

- 用户的发音是否可被理解；
- 用户在真实口语压力下是否敢开口；
- 能力是否能长期保持；
- 学习效果是否能迁移到酒店、机场等其他场景；
- AI 个性化学习路线是否提高长期留存。

## 3. 目标用户

### 3.1 第一目标用户

以简体中文为主要界面语言、近期有出境旅游意愿、认为自己英语不足以独立完成常见交流任务的成年人。

### 3.2 首轮测试招募标准

用户同时满足以下条件才计入有效样本：

1. 年满 18 周岁。
2. 计划或强烈考虑在未来 6 个月内出境旅游。
3. 自评无法独立、流畅地完成英文咖啡店点餐。
4. 能识别英文字母和少量常见单词。
5. 能使用键盘输入简单英文单词或短句。
6. 日常不以英语作为主要工作或生活语言。

### 3.3 本轮不覆盖

- 完全无法识别英文字母或使用英文键盘的用户；
- 能够熟练完成旅行英语交流的用户；
- 以考试、留学或长期系统学习为主要目标的用户；
- 希望训练发音或进行真人口语交流的用户。

这不是长期目标用户的永久排除条件，而是为了保证首轮实验对象与文字问答 MVP 的能力边界一致。

## 4. 待验证的产品假设

### H1：学习效果假设（核心）

目标用户完成一次 AI 引导训练后，在内容不同但任务结构等价的无提示咖啡店任务中，能够比训练前独立完成更多必要交流步骤。

### H2：交互可用性假设

目标用户可以理解 NPC 问题和中文反馈，并在不过度依赖完整示例答案的情况下完成引导训练。

### H3：AI 反馈有效性假设

AI 能够区分“可以完成交流”“能理解但可优化”“必须重试”和“无关回答”，且不会因为非关键语法问题阻断任务。

### H4：主观价值假设

完成训练后，用户对自己独立完成咖啡店文字点餐任务的信心高于训练前，并认为该训练方式与出境需求相关。

### 4.1 本轮不验证的假设

- AI 是否能根据“我要去美国旅游”生成高价值的长期学习路线；
- 多场景内容是否能带来 7 日或更长期留存；
- 订阅会员是否成立；
- 长期 Memory、动态难度或 RAG 是否能提升效果。

## 5. MVP 验证方案

### 5.1 实验结构

```text
用户筛选与匿名 ID
  → 训练前信心评分
  → 基线任务（无纠错、无答案提示）
  → AI 引导训练（允许分级提示和纠错）
  → 迁移任务（等价新内容、无答案提示）
  → 训练后信心评分与结果页
```

### 5.2 三类任务的区别

| 环节 | 目的 | 内容 | 是否纠错 | 是否提供答案 |
| --- | --- | --- | --- | --- |
| 基线任务 | 测量训练前能力 | 咖啡店变体 A | 否 | 否 |
| 引导训练 | 让用户学习并完成任务 | 咖啡店变体 B | 是 | 连续失败后允许 |
| 迁移任务 | 测量训练后独立能力 | 咖啡店变体 C | 否 | 否 |

三个变体都要求表达饮品、规格、定制需求和支付，但商品、NPC 表达和局部情境不同，避免用户只复述刚见过的完整答案。

### 5.3 计分方式

基线任务和迁移任务采用完全相同的评分规则，每个必要步骤计 1 分，总分 0–4：

- 正确表达饮品意图：1 分；
- 正确表达规格意图：1 分；
- 正确表达定制需求或明确表示无需求：1 分；
- 正确回应支付环节：1 分。

`acceptable` 和 `understandable_but_improvable` 均计为该步骤成功；`retry_required` 和 `off_topic` 不计分。评分由 AI 提出结构化结果，确定性规则计算总分。

### 5.4 防止实验失真

- 基线和迁移任务不展示完整推荐答案。
- 迁移任务不复用引导训练中的相同订单内容和完整 NPC 台词。
- 三个任务使用同一评分 schema 和难度等级。
- 用户中途退出或关键数据缺失时标记为无效实验会话，不混入学习效果分母。
- 记录输入法、页面错误和模型异常，区分产品失败与用户能力表现。

## 6. MVP 范围与优先级

### 6.1 P0：验证核心假设所必需

| 编号 | 功能 | 验证作用 |
| --- | --- | --- |
| VFR-01 | 匿名会话与实验记录 | 将三段任务和同一用户关联 |
| VFR-02 | 训练前筛选与信心评分 | 确认有效样本并获得主观基线 |
| VFR-03 | 无提示基线任务 | 获得训练前独立能力分数 |
| VFR-04 | AI 引导训练 | 验证问答、判定、纠错和分级提示 |
| VFR-05 | 无提示迁移任务 | 测量训练后的独立表现 |
| VFR-06 | 结果页 | 呈现能力变化并收集训练后信心 |
| VFR-07 | 场景状态机 | 可靠管理四个必要交流步骤 |
| VFR-08 | AI 结构化判定与降级 | 保证反馈质量和任务可继续性 |
| VFR-09 | 埋点与实验数据导出 | 支持计算核心指标和人工复核 |

### 6.2 P1：核心假设成立后

- AI 学习目标理解；
- 个性化旅行学习路线；
- 机场、酒店、购物等可体验场景；
- 用户账户与跨设备学习记录；
- 多场景个性化 Memory；
- 动态难度和针对性练习；
- 订阅会员与付费能力。

路线页可以用于展示长期产品方向，但不能作为首轮实验的必经步骤，也不能将未开放场景包装成已经可用的个性化结果。

### 6.3 明确不做

- 真人语音聊天和发音评分；
- 社交、商城和积分体系；
- 通用自由聊天；
- 多 Agent 自主规划；
- RAG；
- 复杂课程体系。

## 7. 用户故事

### US-01 理解训练目标

作为一名不擅长英语的出境旅行者，我希望在开始前知道自己要完成什么，以便不用先理解课程术语。

验收：用户能用简体中文看到角色、场景和四项任务目标，并能直接开始。

### US-02 尝试独立表达

作为目标用户，我希望先自己尝试完成一次点餐，以便产品了解我的真实起点，而不是一开始就给答案。

验收：基线任务不显示纠错、句型或完整答案，并产生 0–4 分的完整记录。

### US-03 在真实问答中练习

作为目标用户，我希望店员像真实场景一样逐步提问，以便知道每句话应该在什么时候使用。

验收：NPC 围绕饮品、规格、定制和支付推进，用户主要通过输入简短英文作答。

### US-04 获得可执行的反馈

作为表达不熟练的用户，我希望知道当前说法能否被理解以及如何说得更清楚，以便立即修改，而不是只看到“回答错误”。

验收：反馈只解释当前最关键问题，提供一个可直接使用的表达，并允许用户重试。

### US-05 不因小错误被阻断

作为英语基础薄弱的用户，我希望系统优先判断别人能否理解我，以便不会因为标点或次要语法问题失去信心。

验收：不影响意图理解的问题不能产生 `retry_required`。

### US-06 确认是否真正进步

作为目标用户，我希望训练后再次独立完成一个相似任务，以便确认自己不是仅仅照着答案走完流程。

验收：迁移任务采用新内容且无完整答案，结果页展示训练前后完成步骤变化。

## 8. 核心功能验收

### 8.1 VFR-03 无提示基线任务

- 使用咖啡店变体 A，覆盖四个必要步骤。
- 不显示回答正误、中文纠错、句型提示或示例答案。
- NPC 可以用中性话术继续到下一测试步骤，避免把正确答案泄露给用户。
- 每一步保存用户原始回答、结构化判定和是否得分。
- 用户完成后不立即展示正确答案，避免污染后续训练。

### 8.2 VFR-04 AI 引导训练

- 使用咖啡店变体 B。
- `acceptable` 时自然推进。
- `understandable_but_improvable` 时提供轻量建议并推进。
- `retry_required` 时解释一个关键问题并允许重试。
- 同一步骤连续两次失败后提供句型提示。
- 同一步骤连续三次失败后允许使用完整示例答案，并记录 `example_answer_used=true`。
- AI 不能直接写入完成状态；状态机校验全部必要步骤。

### 8.3 VFR-05 无提示迁移任务

- 使用咖啡店变体 C，不与引导训练重复商品组合和完整台词。
- 不提供句型提示或完整示例答案。
- 使用与基线任务相同的计分规则。
- 完成后计算 `post_score`、`score_gain` 和是否完整独立完成。
- AI 或页面异常导致任务不完整时，不把结果记为用户失败。

### 8.4 VFR-06 结果页

结果页展示：

- 训练前完成步骤数；
- 训练后完成步骤数；
- 本次已经能够表达的任务意图；
- 引导训练中的关键推荐表达；
- 训练后信心评分；
- 重新进行引导训练的入口。

结果页不使用缺乏解释的综合分数，也不宣称用户已经具备真实口语能力。

## 9. 指标与成功标准

### 9.1 核心指标

| 指标 | 定义 | 首轮建议门槛 |
| --- | --- | --- |
| 平均能力增量 | 所有有效用户 `post_score - pre_score` 的平均值 | ≥ 1.0 个步骤 |
| 个人改善率 | `post_score > pre_score` 的有效用户占比 | ≥ 60% |
| 迁移任务完整完成率 | `post_score = 4` 的有效用户占比 | ≥ 50% |
| 引导训练完成率 | 完成引导训练的人数 / 开始引导训练人数 | ≥ 80% |
| 过度辅助率 | 引导训练中使用完整示例答案的人数 / 完成引导训练人数 | ≤ 40% |
| 信心改善率 | 训练后信心至少提高 1 分的有效用户占比 | ≥ 60% |

以上为首轮产品决策阈值，不是行业基准。先通过少量可用性测试校准任务难度，再冻结阈值。

### 9.2 AI 与体验护栏

| 指标 | 门槛 |
| --- | --- |
| 关键槽位提取准确率 | ≥ 95% |
| 错误推进率 | ≤ 2% |
| 结构化输出有效率 | ≥ 99%（含有限重试） |
| 降级后任务可继续率 | 100%（针对已设计异常） |
| 单轮 AI 响应时间 | P95 ≤ 8 秒 |
| 严重内容安全事件 | 0 |

### 9.3 样本与解释限制

- 第一轮非引导测试建议获得至少 30 个有效目标用户样本。
- 该样本用于方向性产品决策，不作为严格的统计学效果证明。
- 在正式测试前先用 5 名目标用户完成可用性测试，排除任务说明、输入方式和页面错误造成的系统性偏差。
- 同时报告均值、分布和典型失败案例，不能只报告总体完成率。

### 9.4 MVP 决策规则

**继续进入产品化：**

- 平均能力增量和个人改善率同时达到门槛；
- AI 错误推进率、结构化输出和安全护栏达标；
- 未出现大量用户因不理解界面而失败。

**继续迭代但不扩场景：**

- 用户表现有改善趋势，但引导完成率、响应速度或 AI 质量未达标；
- 需要先修复交互或判定问题，再重复同一实验。

**重新审视方案：**

- 用户能完成引导训练，但迁移任务没有明显改善；
- 多数改善来自记住完整答案而不是理解交流意图；
- 修复明显可用性和 AI 判定问题后，学习效果仍没有改善趋势。

在核心学习假设成立前，不进入多场景、长期 Memory 或订阅开发。

## 10. 数据与埋点

### 10.1 实验会话核心字段

- anonymous_user_id
- experiment_session_id
- eligibility_result
- pre_confidence（1–5）
- pre_score（0–4）
- guided_completion_type
- example_answer_used
- post_score（0–4）
- post_confidence（1–5）
- score_gain
- task_variant_versions
- AI error and fallback counts
- started_at / completed_at

### 10.2 核心事件

1. `eligibility_completed`
2. `pre_confidence_submitted`
3. `baseline_started`
4. `baseline_step_evaluated`
5. `baseline_completed`
6. `guided_training_started`
7. `guided_hint_used`
8. `guided_example_used`
9. `guided_training_completed`
10. `transfer_started`
11. `transfer_step_evaluated`
12. `transfer_completed`
13. `post_confidence_submitted`
14. `result_viewed`
15. `experiment_abandoned`

### 10.3 数据质量规则

- 基线、引导和迁移任务必须关联同一实验会话。
- 任务变体版本和评分规则版本必须可追溯。
- AI 异常造成的缺失步骤不能按用户错误计分。
- 分析时区分目标用户样本、无效样本和内部测试流量。

## 11. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 用户记住答案而非学会意图 | 虚高学习效果 | 使用等价但不同内容的迁移任务 |
| AI 错误判定 | 分数和反馈失真 | 离线评测、人工抽检、错误推进护栏 |
| 键盘输入能力成为障碍 | 把输入困难误判为英语困难 | 招募标准明确可输入简单英文；记录输入异常 |
| 单场景结果被过度推广 | 得出超出证据的结论 | 明确只验证咖啡店文字任务 |
| 测试用户并非真实目标人群 | 需求结论失真 | 使用统一筛选条件并保存 eligibility 结果 |
| 基线测试引发挫败 | 用户在训练前退出 | 任务说明强调“不是考试”，控制在四个短步骤内 |
| 响应慢打断学习 | 完成率降低 | P95 延迟护栏、处理中状态和降级台词 |
| 匿名数据隐私不清 | 上线风险 | 上线前确认保留周期、删除机制和模型数据条款 |

## 12. 发布与验证阶段

### 阶段 0：内部质量验证

- 完成场景变体、Prompt、状态机和离线评测集；
- 通过 AI 质量护栏和端到端异常用例；
- 不判断产品价值。

### 阶段 1：5 人可用性测试

- 观察用户是否理解任务、反馈和输入方式；
- 修复界面、话术、任务难度和明显判定问题；
- 校准但不为了“达标”随意降低学习门槛。

### 阶段 2：至少 30 人方向性验证

- 使用冻结的任务变体、评分版本和成功门槛；
- 输出核心指标、分布、异常、用户反馈和人工复核结果；
- 按第 9.4 节作出继续、迭代或重新审视决定。

### 阶段 3：产品化

仅在核心学习假设成立后，恢复目标理解和学习路线设计，并逐步增加真实可用场景。

## 13. 依赖项

- 三套难度等价、内容不同的咖啡店任务变体；
- 经人工审核的标准意图、推荐表达和降级台词；
- AI 判定离线评测集及人工标注规范；
- 可导出或查询的实验数据；
- 匿名数据隐私说明；
- 目标用户招募渠道和测试执行人员。

## 14. 待确认事项

1. 是否接受“先验证咖啡店学习效果，目标理解与路线生成降为 P1”的范围调整。
2. 是否接受本版提出的首轮成功阈值，或需要调整其中某项。
3. 网页默认采用响应式、手机优先设计，是否确认。
4. 匿名实验数据的保留周期与清除方式。

## 15. 下一步

1. 确认第 14 节产品决策。
2. 编写咖啡店 A/B/C 三套等价任务内容和评分标注。
3. 输出验证流程的低保真原型。
4. 将现有 AI 功能规格裁剪为首轮验证所需接口。
5. 建立离线评测集并运行第一个模型原型。
